import 'package:flutter/material.dart';

class TxtViewer extends StatelessWidget {
  const TxtViewer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Txt Viewer"),
        centerTitle: true
      ),
      body: const Center(child: Text("Not implemented yet"))
    );
  }

}